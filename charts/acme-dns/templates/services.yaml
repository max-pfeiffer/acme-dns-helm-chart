{{/*
Governing (headless) Service for the StatefulSet. Rendered for both engines on
purpose, so that the name stays stable across an engine switch.
*/}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "acme-dns.fullname" . }}
  labels:
    {{- include "acme-dns.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  clusterIP: None
  # Standard for a governing Service: pods get their DNS record before they pass
  # their readiness probe.
  publishNotReadyAddresses: true
  ports:
    - port: {{ .Values.services.api.ports.api }}
      targetPort: api
      protocol: TCP
      name: api
    - port: {{ .Values.services.dns.ports.dnsTcp }}
      targetPort: dns-tcp
      protocol: TCP
      name: dns-tcp
    - port: {{ .Values.services.dns.ports.dnsUdp }}
      targetPort: dns-udp
      protocol: UDP
      name: dns-udp
  selector:
    {{- include "acme-dns.selectorLabels" . | nindent 4 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "acme-dns.fullname" . }}-api
  labels:
    {{- include "acme-dns.labels" . | nindent 4 }}
    {{- with .Values.services.api.metadata.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with .Values.services.api.metadata.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.services.api.type }}
  ports:
    - port: {{ .Values.services.api.ports.api }}
      targetPort: api
      protocol: TCP
      name: api
  selector:
    {{- include "acme-dns.serviceSelector" . | nindent 4 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "acme-dns.fullname" . }}-dns
  labels:
    {{- include "acme-dns.labels" . | nindent 4 }}
    {{- with .Values.services.dns.metadata.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with .Values.services.dns.metadata.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.services.dns.type }}
  ports:
    - port: {{ .Values.services.dns.ports.dnsTcp }}
      targetPort: dns-tcp
      protocol: TCP
      name: dns-tcp
    - port: {{ .Values.services.dns.ports.dnsUdp }}
      targetPort: dns-udp
      protocol: UDP
      name: dns-udp
  selector:
    {{- include "acme-dns.serviceSelector" . | nindent 4 }}
